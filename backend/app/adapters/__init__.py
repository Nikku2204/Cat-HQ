# Device adapters — one per vendor, arriving M1+. The common interface lives
# in base.py; anything implementing it can replace anything else, including a
# Home Assistant-backed version (the escape hatch in 01-ARCHITECTURE.md).
